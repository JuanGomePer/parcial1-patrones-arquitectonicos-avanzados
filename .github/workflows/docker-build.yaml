name: Build, Push Docker and Update Helm Chart

on:
  push:
    branches:
      - main

jobs:
  build_and_update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract version tag
        id: vars
        run: |
          VERSION=1.0.${{ github.run_number }}-${GITHUB_SHA::7}
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/monolito-inventario:${{ env.VERSION }}

      - name: Checkout manifests repo
        uses: actions/checkout@v3
        with:
          repository: JuanGomePer/manifiestos-parcial1-patrones-arquitectonicos-avanzados
          token: ${{ secrets.GITHUB_TOKEN }}
          path: manifiestos

      - name: Update Helm chart values.yaml image tag
        run: |
          cd manifiestos/charts/patrones
          yq eval ".image.tag = \"${VERSION}\"" -i values.yaml

      - name: Update Chart version in Chart.yaml
        run: |
          cd manifiestos/charts/patrones
          yq eval ".version = \"${VERSION}\"" -i Chart.yaml
          yq eval ".appVersion = \"${VERSION}\"" -i Chart.yaml

      - name: Package Helm Chart
        run: |
          cd manifiestos
          helm package charts/patrones

      - name: Checkout helm-chart repo
        uses: actions/checkout@v3
        with:
          repository: JuanGomePer/helm-chart
          token: ${{ secrets.PAT_HELMCHART }}
          path: helm-chart

      - name: Copy packaged chart to helm-chart repo
        run: |
          cp manifiestos/patrones-*.tgz helm-chart/

      - name: Update Helm repo index
        run: |
          cd helm-chart
          helm repo index . --url https://juangomeper.github.io/helm-chart/

      - name: Commit and push changes to helm-chart repo
        run: |
          cd helm-chart
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "Update Helm chart package and index to ${VERSION}"
          git push origin main
